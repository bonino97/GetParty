import Example from './Example';

export const ExampleConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/parties',
      component: Example,
    },
  ],
};
